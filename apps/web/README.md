This folder hosts the web application for the monorepo.

It mirrors the previous single-repo Next.js app.

Scripts are run from the root using Turbo or directly via `npm --workspace=web run <script>`.
